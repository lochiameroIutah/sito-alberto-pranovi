import { About } from "@/components/About";

export const metadata = { title: "About — Alberto Pranovi" };

export default function AboutPage() {
  return (
    <main className="pt-16">
      <About />
    </main>
  );
}
